#include <stdio.h>

enum flavors {vanilla, chocolate, mint, strawberry};

int main() {
    enum flavors v = vanilla;
    enum flavors c = chocolate;
    enum flavors m = mint;
    enum flavors s = strawberry;

    float cornerOneSales[4];
    float cornerTwoSales[4];

    return 0;
}
